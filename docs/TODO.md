# 待办

> 2026-08-31 收尾时整理。按「阻塞多少后续工作」排序，不按工作量。
> 做完一项就从这里删掉，不要留一堆划掉的行 —— 那样很快就没人看了。

---

## 一、卡在外部，不做别的会一直堵着

### 1. 汽水音乐审核结果

《逆着风跑起来》2026-08-30 23:05 提交，平台承诺 1-2 个工作日。
（2026-08-31 核实：台账仍 reviewing，提交约 2 小时；后台要扫码登录，
agent 无法代查 —— 结果出来需要你扫一次码或等平台通知）

- [ ] 查审核状态，过了就把台账推到 `published`
- [ ] 被拒的话记下**具体原因**到 `configs/platforms.json` 的 qishui 段 ——
      那是下次发布前要先规避的东西
- [ ] 记下**分成比例和结算周期**（在协议正文里，选择页没写）。
      这是当初选「独家代理发行」时唯一没拿到的信息，也是唯一可能让那个决定
      反转的变量

### 2. SFTP 批量上传账号

（2026-08-31 核实：无自动化可做，等你联系汽水管理员申请 ——
机构名 + 抖音 UID 只有你能提供）

单曲发布已经跑通，但**批量**（一次最多 1000 首）要 SFTP 账号。

- [ ] 联系汽水音乐管理员申请，需要提供：机构名称 + 抖音 UID
- [ ] 拿到后填进 `configs/platforms.json` 的 `batch_upload` 段
- [ ] 验证 `voxpublish` 打出来的目录结构和 Excel 能被平台接收

在这之前批量发布这条路是断的。

---

## 二、数据侧（优先级最高的自建工作）

### 3. QQ 音乐同步 ✅ 2026-08-31

网易云那套已经跑通（`scripts/sync_netease.py`），照着做即可。

- [x] 探公开 API —— 旧 c.y.qq.com v8 fcg 端点已 404 废弃，现走
      `u.y.qq.com/cgi-bin/musicu.fcg`（JSON 信封，module/method 指定业务，
      无需登录）。歌手歌曲 `musichall.song_list_server/GetSingerSongList`、
      专辑 `music.musichallAlbum.AlbumListServer/GetAlbumList`
- [x] 实现 `scripts/sync_qq.py`：抓账号（平台键 tencent=腾讯系）、专辑、
      已上架曲目回填台账；专辑封面下到本地；匹配策略照网易云
- [x] 已跑：月栖洲在 QQ 上有 **1 首**（宝贝闺女，你慢慢长大，2026-01-17 发行）
      —— 与网易云 33 首差距巨大，腾讯音乐人那边还没铺开。新曲目已登记，
      标题「宝贝闺女，你慢慢长大」未匹配本地任何作品（是不是你的、要不要
      保留，人工确认）
- [x] 艺人主页已知：`y.qq.com/n/ryqq_v2/singer/002Rcy0a0YpQ7L`

### 4. 台账本身备份到 R2

音频 + 封面同步已落地（`scripts/sync_r2.py`，见 docs/DATA.md「云端同步」），
但 **voxflow.db / configs 仍只在本机** —— 「唯一备份」差最后一块。

- [ ] 把库和关键 config 扔进 R2（music 桶的 `voxflow/` 前缀，或另开私有桶）
- [ ] 恢复演练：从 R2 拉下来能起服务、能读台账

### 5. 台账里只有 1 首有歌词和本地音频 ✅ 2026-08-31（已结论）

37 首作品里 36 首是从网易云回填的，只有标题和平台状态。

- [x] 歌词：`scripts/sync_lyrics.py` 已建并跑过 —— 33 首网易云作品
      **全部无歌词**（API 两端点都验证过，晴天等有词歌能正常返回）。
      结论：这些是纯音乐/器乐作品（专辑名可证：侠骨弦音、沸腾之雪、
      曲目带「伴奏/箫版/洞箫清吹版」），平台本来就没词，不是缺数据。
      将来有新发带词作品，跑一遍脚本即可回填。
- [x] 老作品本地音频判断：**只留云端记录，不补本地音频**。
      网易云已上架 = 永久宿主，播放走平台，本地放一份是冗余；
      需要本地音频的是「平台还没上架、母带只在 Suno 云端」的新作
      （如《逆着风跑起来》—— 已下载到 out/music 并同步 R2）。

---

## 三、代码质量（今天欠的账）

### 6. 剩下 6 个 store 转 TypeScript

已转：`pipeline.ts`。未转：voices / capabilities / tasks / library / suno / synth。

今天六个 bug 全是「字段名/方法名对不上但不报错」，TS 能在编译期挡住。
`src/types/api.ts` 的契约已经写好了，转起来是机械活。

### 7. TypeScript 5.9 → 7

现在锁在 5.9.3，因为 **vue-tsc 3.3.11 还不支持 TS 7**（TS 7 是 Go 重写的
原生编译器，移除了 `lib/tsc` 入口）。硬上的代价是 `.vue` 模板完全没有类型检查。
（2026-08-31 核实：`npm view vue-tsc versions` 最新仍是 3.3.11，无 TS 7 支持，保持阻塞）

- [ ] 定期查 `npm view vue-tsc versions`，出了支持 TS 7 的版本就升
- [ ] 升完跑一次 `npm run typecheck` 确认

### 8. agent-eyes 诊断出的结构问题

它在启动时报了几条，除了 API 层（已做）还剩：

- [ ] 没有 `@` 路径 alias（`vite.config` 的 `resolve.alias` + tsconfig `paths`）
- [ ] 没有 router/pages 层 —— 现在全靠 `currentTab` 一个变量切页面，
      刷新回到默认页，也没法把某一屏的链接发给人
- [ ] 没有 config/constants 层 —— 端口、轮询间隔这些散在各处

---

## 四、产品功能

### 9. 「我的作品」这一屏还很薄

流水线看板能看到阶段和下一步（看板 2026-08-31 从 PublishTab 挪回时已挂载）：

- [x] 从 Suno 下载后自动导入 —— 「我的作品」看板加了「📥 从下载导入」
      弹窗（`/api/inbox` 后端早做好了，界面入口这次补上；实测弹窗列出
      Downloads + out/music 最近一周音频，已在库的标出来）
- [x] 作品详情 —— 看板的「详情」展开本来就有：歌词、风格、各平台状态、
      发布配置一起看（封面在曲目行）
- [ ] 批量操作：多选 → 一起推进阶段（看板目前只有单曲的「下一步」按钮）

### 10. 发布流程只覆盖了汽水音乐

`scripts/publish_qishui.py` 自动填完第 1、2 步。
（2026-08-31 核实：两条都卡在「需要登录实测」—— 网易云入驻绝对手工
且每次入驻机会稀缺，腾讯音乐人零一手资料。没有你的登录会话做不了，
不做无实测的自动化）

- [ ] 网易云的发布自动化（⚠️ **每账号只有 5 次入驻机会，用完永久黑名单** ——
      入驻那段绝对手工，不做自动化）
- [ ] 腾讯音乐人：零一手资料，需要登录实测后再写 `platforms.json`

### 11. 封面走中台重新生成

现在《逆着风跑起来》的封面是中台生成的 1440×1440，但**其余 36 首**用的是
网易云原图（尺寸不一定够平台要求）。

- [ ] 发新平台时按目标平台的尺寸要求重新生成
- [ ] 汽水要 ≥1440×1440，网易云要 ≥1400×1400

---

## 五、清理

- [ ] `tts-webkubor` 这个 Cloudflare Pages 站要删（你说过 TTS 不需要线上网页）
- [ ] `~/.voxflow/configs/*.migrated` —— SQLite 迁移后留的原 JSON，
      确认库没问题后可以删
- [ ] `~/.voxflow/configs.backup-*` —— 迁移前的备份，同上
- [ ] `social-banner.png` 还是改名前的旧图

---

## 不做的事（免得反复讨论）

- **不硬绕 Suno 的下载反爬**。API 的 audio_url 写死 forbidden、CDN 直链 403、
  播放走 blob、下载菜单不响应合成事件。今天在这上面耗了二十多轮 ——
  而下载是一次性动作，Suno 一次出两首你本来就要听过才知道选哪首。
  真正吃时间的是后面（填表、传文件、拼 Excel），那才是自动化该用力的地方。
- **不做全流程无人值守发布**。审核 1-2 个工作日起，自动化把 20 分钟压到
  20 秒，后面还要等 72 小时。而且实名认证和签约的法律效力归属本人，代做没意义。
- **不克隆他人声音**。北京互联网法院「AI 声音侵权第一案」已认定未经许可
  将可识别自然人声音 AI 化构成侵权。克隆本人声音无此问题，但要归档原始录音
  作为授权证据。
